// Utilities
import React from 'react';
import styled from 'styled-components';

// Component

// Styled Comp

const Title = styled.h1`
  color: black;
  border: 2px solid black;
`;


const BoardTitle = ({ text }) => {
  return <Title>Was los</Title>
};

export default BoardTitle;
